version: "3.9"

services:
  mysql:
    image: mysql:8
    platform: linux/amd64
    container_name: mysql-container
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: demo
      TZ: "Asia/Tokyo"
